---
import Image from "@/components/ui/image.astro";

interface Author {
  name: string;
  avatar?: string;
  occupation?: string;
  updatedAt?: string;
}

interface Props {
  content: Author;
  children: any;
}

const { content } = Astro.props;
const { name, avatar, occupation, updatedAt } = content;
---

<div class="divide-y divide-border">
  <div class="space-y-2 pb-8 pt-6 md:space-y-5">
    <h1
      class="text-3xl font-extrabold leading-9 tracking-tight text-foreground sm:text-4xl sm:leading-10 md:text-6xl md:leading-14"
    >
      About Me {updatedAt && `– ${updatedAt}`}
    </h1>
  </div>
  <div
    class="items-start space-y-2 xl:grid xl:grid-cols-3 xl:gap-x-8 xl:space-y-0"
  >
    <div class="flex flex-col items-center space-x-2 pt-8">
      {
        avatar && (
          <Image
            src={avatar}
            alt="avatar"
            width={192}
            height={192}
            className="h-48 w-48 rounded-full"
          />
        )
      }
      <h3 class="pb-2 pt-4 text-2xl font-bold leading-8 tracking-tight">
        {name}
      </h3>
      {occupation && <div class="text-muted-foreground">{occupation}</div>}
    </div>
    <div class="prose max-w-none pb-8 pt-8 dark:prose-invert xl:col-span-2">
      <slot />
    </div>
  </div>
</div>
